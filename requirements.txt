# Core deep-learning stack.
# Install PyTorch and torchvision from https://pytorch.org/get-started
# (the deformable-attention op is compiled against your local CUDA/PyTorch,
# see models/ops/make.sh).
# PyTorch 2.5.1 + torchvision 0.20.1 recommended.

torch==2.5.1
torchvision==0.20.1

numpy
opencv-python
Pillow
pycocotools
tqdm
cython
scipy
matplotlib
